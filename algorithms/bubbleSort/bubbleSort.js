module.exports.bubbleSort = array => {
  // for i is 0 to i less than the last index of the array
  //  for j is 0 to j less than the last index - i already sorted elements
  //    if element at j more than element at j + 1
  //      swap element at j and j + 1
  // return array
};
