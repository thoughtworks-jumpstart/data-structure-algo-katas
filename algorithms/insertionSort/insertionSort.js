module.exports.insertionSort = array => {
  // for i from 1 to less than length of array
  //   assign variable key to array[i]
  //   assign variable j to i - 1
  //   while j more than equal to 0 and array[j] more than key
  //     shift array[j] behind to array[j + i]
  //     decrease j by 1
  // (finish shifting back!)
  // insert key at the correct position array[j + 1]
  // return array
};

// use edge case such as using a reversed array to fully appreciate why it
// is more than equal to 0 and not just more than 0
