module.exports = class Stack {
  constructor() {
    // set stack to empty array
  }

  push(item) {
    // push item to stack
  }

  pop() {
    // pop item from stack
  }

  peek() {
    // peek at the item at the top of the stack
  }
};
